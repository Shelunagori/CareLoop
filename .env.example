# CareLoop — environment template. Placeholders only; never commit real values.
# Copy to .env.local and fill in.

# --- Supabase (M0) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only. Bypasses RLS. Must never be imported into a client component.
SUPABASE_SERVICE_ROLE_KEY=

# --- Not implemented in M0. Documented future configuration only. ---
# OpenAI: conversational turn, extraction, family render, reply parse (M1+)
OPENAI_API_KEY=
# ElevenLabs: declared as a port in docs/05 §15; no implementation planned for V1
ELEVENLABS_API_KEY=
